on:
  pull_request:
    # Don't try this, it doesnt work lol, it looks at the target branch to match regex
    # branches-ignore:
    #   - "ignore/**"

name: Test branch ignore

jobs:
  build:
  if: github.ref == 'ignore/**'
    runs-on: ubuntu-latest

    name: Test ignore step
    steps:
      - name: echo
        run: |
          echo "helloworld"
